{% extends 'base.html.twig' %}

{% block title %}Distributors index{% endblock %}

{% block body %}
    <h1>Distributors index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name_distributor</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for distributor in distributors %}
            <tr>
                <td>{{ distributor.id }}</td>
                <td>{{ distributor.nameDistributor }}</td>
                <td>
                    <a href="{{ path('app_distributors_show', {'id': distributor.id}) }}">show</a>
                    <a href="{{ path('app_distributors_edit', {'id': distributor.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_distributors_new') }}">Create new</a>
{% endblock %}
